<resources>
    <!-- Base application theme. Compose draws all the UI; this just tells
         Android which system theme family to start from (light/dark,
         no legacy action bar). -->
    <style name="Theme.BulkTextReply" parent="android:Theme.Material.Light.NoActionBar" />
</resources>
